{
  "name": "eds-mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing the Edwson Design System (component library, tokens, icons, brand rules) to LLM clients.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "eds-mcp-server": "dist/index.js"
  },
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ]
}
